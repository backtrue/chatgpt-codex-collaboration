interface:
  display_name: "ChatGPT-Codex macOS 協作驗收"
  short_description: "在 Mac 上讓 ChatGPT 實作、Codex 驗收並持續追蹤原生 /goal"
  default_prompt: "依照 macOS-first 協作流程，先執行 Mac Doctor，再檢查目前 Codex thread 的原生 /goal；若不存在就依使用者要求建立，若存在就綁定並沿著完整 objective 推進。每次只把一個有限任務交給 ChatGPT 實作，以 launchd 等待 GitHub handoff；Codex 驗收後回到原生 goal audit，未完成就保持 active 並繼續。"
